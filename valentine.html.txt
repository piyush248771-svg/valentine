
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Harleen ❤️</title>

<style>
  body {
    margin: 0;
    height: 100vh;
    background: linear-gradient(135deg, #ff5f8d, #ff85b3);
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: Arial, sans-serif;
  }

  .card {
    background: #ffffff;
    width: 90%;
    max-width: 360px;
    padding: 30px 20px;
    border-radius: 22px;
    text-align: center;
    box-shadow: 0 15px 30px rgba(0,0,0,0.2);
  }

  h1 {
    color: #ff3366;
    margin-bottom: 10px;
  }

  h2 {
    color: #444;
    margin-bottom: 25px;
  }

  .buttons {
    position: relative;
    height: 150px;
  }

  button {
    border: none;
    border-radius: 30px;
    padding: 14px 28px;
    font-size: 18px;
    cursor: pointer;
    transition: transform 0.3s ease;
  }

  #yes {
    background: #ff3366;
    color: #fff;
  }

  #no {
    background: #ddd;
    color: #333;
    position: absolute;
    top: 70px;
    right: 20px;
  }
</style>
</head>

<body>

<div class="card">
  <h1>Harleen ❤️</h1>
  <h2>Will you be my Valentine?</h2>

  <div class="buttons">
    <button id="yes" onclick="yesClicked()">Yes 💖</button>
    <button id="no" onclick="noClicked()">No 😢</button>
  </div>

  <p style="font-size:12px;color:#777;margin-top:15px;">
    Tap anywhere to play music 🎶
  </p>
</div>

<audio id="music" loop>
  <source src="https://www.bensound.com/bensound-music/bensound-love.mp3" type="audio/mpeg">
</audio>

<script>
  let yesScale = 1;
  let noScale = 1;
  let musicStarted = false;

  document.body.addEventListener("click", function () {
    if (!musicStarted) {
      const music = document.getElementById("music");
      music.play().catch(() => {});
      musicStarted = true;
    }
  });

  function noClicked() {
    noScale -= 0.15;
    yesScale += 0.15;

    if (noScale <= 0.2) {
      document.getElementById("no").style.display = "none";
    }

    document.getElementById("no").style.transform = `scale(${noScale})`;
    document.getElementById("yes").style.transform = `scale(${yesScale})`;
  }

  function yesClicked() {
    document.body.innerHTML = `
      <div style="
        height:100vh;
        display:flex;
        justify-content:center;
        align-items:center;
        background:linear-gradient(135deg,#ff5f8d,#ff85b3);
        color:white;
        text-align:center;
        font-size:28px;
        padding:20px;">
        YAYYYY 💕<br>Harleen is my Valentine 🥰🌹
      </div>
    `;
  }
</script>

</body>
</html>